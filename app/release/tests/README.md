# Release assembly tests

Scaffold only. Reserved for packaging checks that prove platform-matching sidecars, model assets, manifest files, verification key, and signature material are present in a candidate bundle. The separately trusted pre-install verifier is checked through the Release Verification module interface.

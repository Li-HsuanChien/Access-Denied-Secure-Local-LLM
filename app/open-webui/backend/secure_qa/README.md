# Secure QA backend features

Scaffold only. The Python package here will hold the four SDD workflow modules and supporting diagnostics, each behind the interface in the [codebase design](../../../../docs/superpowers/specs/2026-09-22-secure-local-document-qa-codebase-design.md). The upstream `backend/open_webui/` host will only register thin transport routes and restrict capabilities. No Python implementation is present.
